# Tasks
- [x] Task 1: 代码库勘察与技术选型锁定
  - [x] 确认项目当前运行方式与包管理（Node 版本、脚本、现有依赖）
  - [x] 确认图片渲染实现路径（Node 直接绘制 vs HTML 截图），V1 选择“直接绘制”
  - [x] 确认 CLI 框架与参数解析方式（沿用项目现有模式）

- [x] Task 2: 实现 CLI 输入输出骨架（Render-only）
  - [x] 支持从 stdin 读取文本
  - [x] 支持 `--in <file>` 从文件读取文本
  - [x] 支持 `--out <path>` 指定输出文件/前缀与输出目录校验
  - [x] 支持 `--format png|jpg`（默认 png）
  - [x] 支持 `--theme terminal|paper`（至少两种）
  - [x] 参数非法时返回非 0 退出码与可读错误信息

- [x] Task 3: 文本清洗与排版（确定性）
  - [x] 实现换行规范化（`\r\n`/`\n`）
  - [x] 实现控制字符清理策略（保留 `\n`，`tab` 采用确定性展开策略）
  - [x] 实现 `\r` 覆盖行处理的默认策略（确定性且提升可读性）
  - [x] 实现宽度策略（默认列宽或默认像素宽二选一，并提供参数覆盖）
  - [x] 实现自动换行与渲染行列表产出
  - [x] 实现分页（按固定行数或固定像素高度拆分页面）

- [x] Task 4: 图片渲染与主题
  - [x] 基于等宽字体将渲染行绘制到图片（边距、行距、背景色、前景色）
  - [x] 实现 `terminal` 与 `paper` 两套基础主题
  - [x] 实现多页输出命名规则（`name-001.ext`）
  - [x] 加入字体资源打包与回退逻辑（优先固定开源等宽字体）

- [x] Task 5: 测试与验证
  - [x] 单元测试：文本清洗、换行、`\r` 策略、换行与分页
  - [x] CLI E2E：stdin → 输出文件存在且尺寸正确
  - [x] CLI E2E：`--in` 文件 → 输出分页命名正确、页数符合预期
  - [ ]（可选）Golden：验证输出尺寸/页数/关键行落页（避免像素级漂移）

# Task Dependencies
- Task 2 depends on Task 1
- Task 3 depends on Task 2
- Task 4 depends on Task 3
- Task 5 depends on Task 4
