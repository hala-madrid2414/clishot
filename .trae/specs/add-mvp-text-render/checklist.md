* [x] CLI 支持 stdin 与 `--in <file>` 两种输入源，错误场景返回非 0 且信息可读

* [x] `--out <path>` 支持单页与多页命名规则（`name-001.ext`），目录不可写时报错

* [x] 文本清洗对 `\r\n`/`\n` 规范化、不可见控制字符清理、`\r` 策略均为确定性实现

* [x] 默认宽度策略生效且可通过参数覆盖；超过宽度时自动换行

* [x] 分页策略生效：超长内容输出多页且顺序正确

* [x] 至少两套主题可用：`terminal` 与 `paper`

* [x] 输出支持 PNG 与 JPG（默认 PNG），生成图片尺寸符合预期且可打开查看

* [x] 单元测试覆盖清洗/换行/分页核心逻辑；CLI E2E 测试覆盖 stdin 与 `--in`
